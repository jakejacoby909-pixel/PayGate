export const metadata = {
  title: "Terms of Service — PayGate",
  description: "PayGate terms of service. Read our terms for using the PayGate checkout page builder platform.",
  alternates: {
    canonical: "https://pay-gate.dev/terms",
  },
};

export default function TermsLayout({ children }) {
  return children;
}
