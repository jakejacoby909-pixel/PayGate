export const metadata = {
  title: "Log In — PayGate | Checkout Page Builder",
  description:
    "Log in to PayGate to manage your checkout pages, track sales, and customize your payment links.",
};

export default function LoginLayout({ children }) {
  return children;
}
